services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: exporter-ease-app
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env.docker
    environment:
      - NODE_ENV=production
    restart: always
